<%= form_for(@airplane) do |f| %>
  <% if @airplane.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@airplane.errors.count, "error") %> prohibited this airplane from being saved:</h2>

      <ul>
      <% @airplane.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :row %><br>
    <%= f.number_field :row %>
  </div>
  <div class="field">
    <%= f.label :column %><br>
    <%= f.text_field :column %>
  </div>
  <div class="field">
    <%= f.label :plane_num %><br>
    <%= f.text_field :plane_num %>
  </div>
  <div class="field">
    <%= f.label :plane_type %><br>
    <%= f.text_field :plane_type %>
  </div>
  <div class="actions">
    <%= f.submit :class => "btn blue darken-4"  %>
  </div>
<% end %>
